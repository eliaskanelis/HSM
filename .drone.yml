kind: pipeline
type: docker
name: Test

platform:
  os: linux
  arch: amd64

steps:

- name: Change permissions to user
  image: drone/git
  commands:
  - chown -R 1000:1000 .

- name: Build
  image: voidbuffer/devtools:latest
  commands:
  - make
